// const autenticado = true;
// const puedePagar = true;

// console.log(autenticado ? `Si esta autenticado` : `No esta autenticado`);
// console.log(autenticado ? puedePagar? `Si esta aut y puede pagar` : `Si esta autenticado, no puede pagar` : `No esta autenticado`);